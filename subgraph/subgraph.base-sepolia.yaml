specVersion: 1.0.0
description: Helios — Base Sepolia execution surface (Phase 5 / WS6).
repository: https://github.com/helios-market/helios
schema:
  file: ./schema.graphql

# Phase 5 / WS6 — Base Sepolia subgraph. Goldsky's multi-chain story
# is "one subgraph per chain, compose at query time" rather than a
# single composite index, so the execution-chain datasources live in
# this sibling manifest. Frontend (WS7) fans out parallel queries
# against the three Goldsky endpoints and merges by entity id —
# `Strategy.id` is the registry address, identical on every chain.
#
# Addresses + startBlock are sentinels; replaced after WS2 broadcast
# writes `contracts/deployments/base-sepolia.json`. Goldsky deploy is
# gated until that broadcast lands.

dataSources:
  - kind: ethereum
    name: StrategyVault
    network: base-sepolia
    source:
      address: "0x000000000000000000000000000000000000dEaD"
      abi: StrategyVault
      startBlock: 0
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/strategy-vault.ts
      entities:
        - Trade
        - NAVSnapshot
        - Strategy
      abis:
        - name: StrategyVault
          file: ./abis/StrategyVault.json
      eventHandlers:
        - event: TradeAttested(indexed address,indexed address,indexed bytes32,bytes32,address,address,uint256,uint256,uint8,uint64,uint64)
          handler: handleTradeAttested
        - event: NAVReported(indexed address,uint256,uint64)
          handler: handleNAVReported
        - event: Slashed(indexed address,uint256,string)
          handler: handleSlashed

  - kind: ethereum
    name: HeliosOApp
    network: base-sepolia
    source:
      address: "0x000000000000000000000000000000000000dEaD"
      abi: HeliosOApp
      startBlock: 0
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/helios-oapp.ts
      entities:
        - CrossChainReputationMessage
      abis:
        - name: HeliosOApp
          file: ./abis/HeliosOApp.json
      eventHandlers:
        - event: AttestationQueued(indexed address,indexed uint64,uint256)
          handler: handleAttestationQueued
        - event: AttestationsFlushed(indexed uint32,uint256,uint64,uint64,bytes32)
          handler: handleAttestationsFlushed
        - event: ReputationMessageSent(indexed uint32,indexed address,uint8,bytes32)
          handler: handleReputationMessageSent
