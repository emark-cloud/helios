[project]
name = "helios-contracts-abi"
version = "0.1.0"
description = "Python ABI constants for Helios contracts. Auto-generated from Foundry artifacts; do not edit."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/helios_contracts_abi"]
