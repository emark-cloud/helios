# Auto-generated by `helios scaffold-strategy momentum_v1`. Wraps a
# single-package strategy built on `helios-strategy-sdk`. The runtime
# loop (oracle polling, prover round-trip, on-chain submission) is
# wired by `helios deploy --strategy src/{{NAME_SNAKE}}/strategy.py`.
#
# Operators with extra deps should add them to pyproject.toml.
FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY pyproject.toml /app/pyproject.toml
COPY src /app/src

RUN pip install .

CMD ["python", "-m", "{{NAME_SNAKE}}"]
