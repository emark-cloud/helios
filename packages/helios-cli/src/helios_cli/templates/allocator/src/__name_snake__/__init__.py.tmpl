"""{{NAME_PASCAL}} — Helios allocator scaffold.

Subclass surface lives in `allocator.py`. The runtime entrypoint is
`python -m {{NAME_SNAKE}}` (see `__main__.py`).
"""

from {{NAME_SNAKE}}.allocator import {{NAME_PASCAL}}Allocator

__all__ = ["{{NAME_PASCAL}}Allocator"]
__version__ = "0.1.0"
