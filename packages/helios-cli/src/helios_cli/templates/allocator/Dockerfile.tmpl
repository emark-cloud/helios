# Auto-generated by `helios-allocator init`. Wraps a single-package
# allocator built on `helios-allocator-sdk`. The runtime entrypoint
# is `python -m {{NAME_SNAKE}}` (see src/{{NAME_SNAKE}}/__main__.py).
#
# Operators with extra deps should add them to pyproject.toml; the
# build step picks them up via `pip install .`.
FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY pyproject.toml /app/pyproject.toml
COPY src /app/src

RUN pip install .

CMD ["python", "-m", "{{NAME_SNAKE}}"]
